/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "res.cloudinary.com",
            "static.vecteezy.com",
        ],
    },
};

module.exports = nextConfig;
